<style>
  .db-list-info{
    padding
  }

  #db-list-info-{{section.id}}{
    background-color: {{ section.settings.background_color }};
    padding: 5%;
    height: 303px;
    width: 100%;
    display: grid;
    align-items: center;
    justify-items: center;
  }
  .db-list-info .db-list-info__content-wrapper {
    width: 900px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    justify-content: center;
    gap: 54px;
    align-items: center;
    height: 100%;
  }
  .db-list-info .db-list-info__image-content {
    position: relative;
    width: 120px;
    height: 120px;
    display: flex;
    border-radius: 50%;
    justify-content: center;
    align-items: center;
    background-color: rgb(255, 255, 255);
  }
  .db-list-info .db-list-info__image,
  .placeholder-image {
    width: 100%;
    height: auto;
    object-fit: cover;
    z-index: 1;
  }
  .db-list-info .db-list-info__item {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 13px;
  }
  .db-list-info  .db-list-info__label {
    width: 184px;
    height: 55px;
    padding: 0 10px;
    color: #fff;
    font-size: 20px;
    font-family: "Roboto slab"
    , serif;
    line-height: 1.18;
    font-weight: bold;
  }

  @media (max-width: 768px) {
    #db-list-info-{{section.id}}{
      height: max-content;
    }

    .db-list-info .db-list-info__content-wrapper {
      width: 100%;
      height: max-content;
      grid-template-columns: 1fr 1fr;
      row-gap: 45px;
      justify-items: center;
    }
    .db-list-info__item{
        width: calc(50% - 45px);
      }
  }

  @media (max-width: 480px) {
    .db-list-info .db-list-info__content-wrapper {
      grid-template-columns: 1fr;
      gap: 30px;
    }
    .db-list-info__item{
        width: 100%;
      }
  }
</style>

<div id="db-list-info-{{section.id}}" class="
    db-list-info
    color-{{ section.settings.color_scheme }}
  ">
  <div class="db-list-info__content-wrapper">
    {% for block in section.blocks %}
      <div class="db-list-info__item" {{ block.shopify_attributes }}>
        {% if block.settings.image != blank %}
          <div class="db-list-info__image-content">
            {{ block.settings.image
 | image_url: width: 400
 | image_tag: loading: 'lazy', class: 'db-list-info__image'
            }}
          </div>
        {% else %}
          {{ 'image' | placeholder_svg_tag: 'placeholder-image' }}
        {% endif %}
        <h3 class="db-list-info__label">{{ block.settings.label }}</h3>
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
  {
    "name": "db list info",
    "settings": [
      {
        "type": "select",
        "id": "section_width",
        "label": "t:settings.width",
        "options": [
          {
            "value": "page-width",
            "label": "t:options.page"
          }, {
            "value": "full-width",
            "label": "t:options.full"
          }
        ],
        "default": "full-width"
      }, {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "t:settings.color_scheme",
        "default": "scheme-1"
      }, {
        "type": "color",
        "id": "background_color",
        "label": "Background color",
        "default": "#C20502"
      }
    ],
    "blocks": [
      {
        "type": "list_info",
        "name": "list info",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          }, {
            "type": "text",
            "id": "label",
            "label": "Label:"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "list info:",
        "blocks": [
          {
            "type": "list_info",
            "settings": {
              "label": "BUILDING MATERIALS"
            }
          }, {
            "type": "list_info",
            "settings": {
              "label": "FLOORING TIMBER"
            }
          }, {
            "type": "list_info",
            "settings": {
              "label": "DECKING TIMBER"
            }
          }, {
            "type": "list_info",
            "settings": {
              "label": "FENCING TIMBER"
            }
          }
        ]
      }
    ]
  }
{% endschema %}